interface:
  display_name: "Agent-First Config"
  short_description: "Safely inspect and edit config files"
  default_prompt: "Use $agent-first-config to inspect or safely modify this configuration while preserving format and protecting secrets."
