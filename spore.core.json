{
  "$schema": "https://cmn.dev/schemas/v1/spore-core.json",
  "id": "agent-first-config",
  "name": "Agent-First Config",
  "version": "0.2.0",
  "domain": "agentfirstkit.com",
  "key": "ed25519.BwbSzqdDYyMN1pPW5snRk3afrJAovHn8aueMvDpfNKVw",
  "synopsis": "Generic dot-path accessor for JSON/TOML/YAML/dotenv/INI configs, usable both as a Rust library and as a standalone afconfig CLI binary. The library is the primary interface: spores embed it for generic dot-path access without writing a dispatch table per field. The CLI wraps the same logic for shell scripts and quick one-off reads or edits from any language.",
  "intent": [
    "Provide a reusable config traversal and mutation library that works with any serde struct",
    "Enable dot-path get/set operations (e.g., imap.host, identities.me.email) without enumeration in Clap",
    "Support slug-based array access (keyed lists) alongside nested object and scalar paths",
    "Keep the lib lightweight with independently gated JSON, TOML, YAML, dotenv, and INI backends",
    "Publish the agent-first-config skill so agents use afconfig for safe reads and mutations",
    "Validate arbitrary serde config values through a fallible typed adapter",
    "Preserve unrelated source bytes during supported JSON, TOML, YAML, dotenv, and INI edits",
    "Solve the consistency problem in multi-format tools where config CLI patterns diverge"
  ],
  "license": "MIT",
  "mutations": [
    "Rework config mutations to preserve untouched source bytes across JSON, TOML, YAML, dotenv, and INI, failing safely when an edit cannot preserve the document",
    "Add side-effect-free dotenv and INI Core v1 backends alongside independently selectable format features, while shipping the afconfig CLI in the default installation",
    "Add secure secret input sources and configurable field redaction so sensitive values stay out of arguments, logs, results, and errors",
    "Add scalar-only shell output and route normal CLI results and failures through consistent Agent-First Data JSON, YAML, and plain formatting",
    "Add typed serde conversion, lossless unsigned integers, generic keyed-list operations, and an unambiguous escaped dot-path grammar",
    "Write mutations atomically while preserving file modes and refusing symlink or hardlink targets",
    "Expand the bundled skill and documentation with Rust library workflows, minimal-feature installation, and source-preserving mutation guidance",
    "Raise the MSRV to Rust 1.88 and make container, Windows, macOS, feature-matrix, and fixed-entrypoint CI checks reliable"
  ],
  "bonds": [],
  "tree": {
    "algorithm": "blob_tree_blake3_nfc",
    "exclude_names": [
      ".git",
      ".cmn",
      "target"
    ],
    "follow_rules": [
      ".gitignore"
    ]
  }
}
